import Operator from "../../src/js/Components/Operator";

let operator = new Operator(["15", "+", "20"]);

describe("클래스 Operator 테스트", () => {
  beforeEach(() => {
    operator = new Operator(["15", "+", "20"]);
  });

  test("calculate 함수는 연산자에 따라 연산을 진행합니다.", () => {
    expect();
  });
});
